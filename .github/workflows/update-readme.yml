# Scheduled runs use UTC. :00 and :30 each hour UTC ≈ every 30 minutes.
# If schedules never run: repo Settings → Actions → General → enable Actions;
# Workflow permissions → Read and write. Forks: enable Actions + allow schedules.
name: Update README

on:
  schedule:
    - cron: "0 * * * *"
    - cron: "30 * * * *"
  workflow_dispatch:

concurrency:
  group: profile-readme-activity
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update-readme-with-activity:
    name: Update profile recent activity
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Render Recent Activity into README
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ACTIVITY_USERNAME: ${{ github.repository_owner }}
          IGNORED_REPOS: "hongzhi-gao/hongzhi-gao"
          MAX_ACTIVITY_LINES: "10"
          README_FILE: "README.md"
        run: python3 scripts/update_recent_activity.py

      - name: Commit README if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md
          if git diff --cached --quiet; then
            echo "No README changes to commit"
            exit 0
          fi
          git commit -m "chore(readme): update recent activity"
          git push
